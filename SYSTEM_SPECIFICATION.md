# Auto-Shutter システム仕様書

## 1. システム概要
**Auto-Shutter** は、Raspberry Pi上で動作するインタラクティブな自律型撮影ロボットです。コンピュータビジョン（MediaPipe Pose）を使用してユーザーを検出し、最適なショットのために物理的な位置を自動調整し、ジェスチャーコマンドを受け取って写真を撮影します。撮影後は即座にQRコードで写真を共有する、シームレスな「フォトブース」体験を提供します。

## 2. ハードウェア仕様

### 2.1 演算ユニット
- **デバイス**: Raspberry Pi (スムーズなAI推論のために Model 4 または 5 を推奨)。
- **OS**: Raspberry Pi OS (Debianベース)。

### 2.2 周辺機器
- **カメラ**: USBウェブカメラ または Raspberry Piカメラモジュール。
    - フォーマット: YUYV (一部のPi環境で推奨)。
    - 解像度: 640x480 (パフォーマンス優先設定)。
    - フレームレート: 15 FPS (Pi上でのMediaPipe動作に最適化)。
- **モーター**: モータードライバー付きDCモーター (差動駆動)。
    - **制御ライブラリ**: `gpiozero`。
    - **ピン配置**:
        - 左モーター: GPIO 17 (前進), GPIO 18 (後退)
        - 右モーター: GPIO 19 (前進), GPIO 20 (後退)

## 3. ソフトウェアアーキテクチャ

### 3.1 技術スタック
- **言語**: Python 3
- **ビジョン & AI**: 
    - **OpenCV (`cv2`)**: 画像のキャプチャ、加工、UI描画。
    - **MediaPipe**: リアルタイム姿勢推定 (BlazePose)。
        - `model_complexity=0` (Lite) に設定し、Piでのパフォーマンスを確保。
- **ハードウェア制御**: `gpiozero` によるモーターGPIO操作。

### 3.2 主要モジュール
- **`main.py`**: コアとなるステートマシンアプリケーション (`PhotoBoothApp`)。メインループ、モード遷移、ハードウェア連携を管理します。
- **`detect_circle_gesture.py`**: AIロジックをカプセル化しています。
    - **円ジェスチャー検出**: 手首、肘、肩の相対位置を確認し、「丸」のポーズを判定します。
    - **エッジ/近接検出**: バウンディングボックスを計算し、被写体が遠すぎる、近すぎる、または中心からずれているかを判定します。
- **`ui_overlay.py`**: カメラ映像上に描画される「ヘッドアップディスプレイ (HUD)」を管理し、テキストやステータス表示の可読性を確保します。
- **`simple_server_qr.py`**: ファイル共有のための一時的なローカルHTTPサーバーを起動し、QRコードを生成します。

## 4. 動作フロー（ユーザー体験）

システムは以下のフェーズを持つステートマシンとして動作します：

### フェーズ 1: READY（待機）
*ロボットはアイドル状態で、ユーザーを待っています。*
- **画面表示**: "Make a Circle to Start"（丸を作ってスタート）。
- **ロジック**: 常時「円」ジェスチャー（腕を頭上で組み、手首を合わせる）を監視します。
- **アクション**: ジェスチャーを検知すると、**ADJUST** フェーズへ遷移します。

### フェーズ 2: ADJUST（自動構図調整）
*ロボットが物理的に移動し、ユーザーをフレームに収めます。*
- **期間**: 5秒間（設定可能）。
- **画面表示**: "Adjusting..."（調整中）、"Coming Closer..."（接近中）、"TOO CLOSE"（近すぎます）。
- **ロボット動作ロジック（優先順位順）**:
    1.  **後退**: ユーザーが上端、または左右両端に触れている場合（近すぎる）。-> **後退 (Backward)**。
    2.  **センタリング**:
        -   ユーザーが**右端**にいる -> **右旋回** (左モーター前進)。
        -   ユーザーが**左端**にいる -> **左旋回** (右モーター前進)。
    3.  **接近**: 被写体の幅が画面幅の30%未満の場合（遠すぎる）。-> **前進 (Forward)**。
- **アクション**: 時間経過後、**TAKE_PICTURE** フェーズへ遷移します。

### フェーズ 3: TAKE_PICTURE（撮影）
*撮影フェーズです。*
- **画面表示**: "Pose for Picture!"（ポーズをとって！）、残り枚数。
- **ロジック**: シャッターを切るための「円」ジェスチャーを待ちます。
- **タイムアウト**: 30秒間ジェスチャーがない場合、**READY** に戻ります。
- **シーケンス**:
    1.  **ジェスチャー検知**: 3秒間のカウントダウンを開始。
    2.  **撮影**: 写真を `captured_photos/` に保存。
    3.  **クールダウン**: "Nice Shot!" を表示し、2秒待機。
    4.  **繰り返し**: `MAX_PICTURE`（デフォルト3枚）に達するまで繰り返します。
- **アクション**: すべての撮影が完了すると、**RESULT** フェーズへ遷移します。

### フェーズ 4: RESULT（結果表示・共有）
*思い出の共有。*
- **期間**: 20秒間。
- **画面表示**: 中央にQRコードを表示。
- **バックグラウンド処理**:
    -   `captured_photos` ディレクトリをホストするローカルPython HTTPサーバーを起動。
    -   ローカルIPアドレス（例: `http://192.168.x.x:8000`）へのQRコードを生成。
- **アクション**: 時間経過後、サーバーを停止し、**READY** に戻ります。

## 5. UIとフィードバック設計
- **鏡像表示**: UX向上のため、カメラ映像は左右反転（鏡として動作）して表示されます。
- **視覚スタイル**:
    -   **ヘッダー**: 現在のフェーズと、タイムアウト/ステータスを表示。
    -   **フッター**: 操作指示とプログレスバー。
    -   **中央ステータス**: 重要な警告（例：「TOO CLOSE」）は赤色、カウントダウンは緑色で表示。
    -   **QRコード**: 読み取りやすくするために、白い枠線を付けて表示。
